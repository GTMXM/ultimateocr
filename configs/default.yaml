project:
  name: UltimateOCR
  version: 0.1.0
  seed: 42
hardware:
  device: cuda
  num_workers: 2
  mixed_precision: true
training:
  batch_size: 16
  epochs: 100
  learning_rate: 0.0001
  weight_decay: 0.0001
dataset:
  image_size: 640
  num_classes: 1
